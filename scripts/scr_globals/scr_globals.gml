#macro UNIT 16
#macro SIZE 512

#macro K_LEFT keyboard_check(vk_left) || keyboard_check(ord("A"))
#macro K_RIGHT keyboard_check(vk_right) || keyboard_check(ord("D"))
#macro K_JUMP keyboard_check(vk_space)
